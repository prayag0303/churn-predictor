services:
  - type: web
    name: churn-predictor-api
    runtime: python
    pythonVersion: 3.11
    plan: free
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
